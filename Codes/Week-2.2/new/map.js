// MAP is used to create keyvalue pairs.

const map = new Map();
map.set("name", "Harkirat");
map.set("age", 30);

const firstName = map.get("name");
console.log(firstName);

// console.log(map["name"]);